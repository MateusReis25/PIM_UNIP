<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.FolhaPagamento" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.FolhaPagamento" parent="Base.Theme.FolhaPagamento" />

    <style name="Theme.FolhaPagamento.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.FolhaPagamento.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.FolhaPagamento.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>